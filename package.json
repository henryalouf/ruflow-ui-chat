{
  "name": "ruflow-ui",
  "version": "1.0.0",
  "description": "Ruflow Chat Interface - Web UI for Claude Code CLI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "test:server": "RUFLOW_OPEN=1 PORT=3099 node server.js & SRV=$!; sleep 4; node --test tests/*.test.js; R=$?; kill $SRV 2>/dev/null; exit $R"
  },
  "dependencies": {
    "express": "^4.18.2",
    "jsdom": "^29.1.1",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.0",
    "ws": "^8.16.0"
  }
}
