node_modules/

# Runtime state — never commit
.env
*.db
HEARTBEAT.md

# User data
sessions/*
!sessions/.gitkeep
uploads/*
!uploads/.gitkeep
memory/*
!memory/.gitkeep

# Projects (instructions + knowledge) — runtime state
projects/*
!projects/.gitkeep
.cache/
